version: '2.7'


services:
  db:
    build:
      context: .
      dockerfile: Dockerfile.dbtest
    volumes: 
      - postgres_data:/var/lib/postgresql/data/
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: >
      sh -c "python manage.py migrate &&
             gunicorn gifts_shaker.wsgi:applicatin --bind 0.0.0.0:8000"
    ports:
      - "8000:8000"
    depends_on: 
      - db

volumes:
  postgres_data:
